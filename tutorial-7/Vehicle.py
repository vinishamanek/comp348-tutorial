# create a Vehicle class with
# max_speed and mileage as instance attribute

# create a child classes Bus and Car that will inherit 
# all variables and methods rom the Vehicle class

# add a property that every sincle class instance should have
# let this property be the class attribute color, with default value white