#include <stdio.h>

// void swap(...) {
//     // implement swap
// }


int main() {
    int x = 5, y = 10;
    
    printf("initial values: x=%d, y=%d\n", x, y);
    
    // swap(...); 
    printf("swapped values: x=%d, y=%d\n", x, y);  
    
    return 0;
}